{
  "name": "@jdo/order-state-machine",
  "version": "0.1.0",
  "description": "订单状态机（纯函数，前后端共用）— 单一真相，对应 openspec/specs/order",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
