# deps
node_modules/
.pnpm-store/

# build
dist/
build/
.turbo/
*.tsbuildinfo

# env & secrets (真密钥永不入仓)
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# store 运行时快照（ADR-0014；STORE_PERSIST_PATH 默认 ./.data）
.data/

# openspec 本地临时
openspec/**/.tmp/

# claude 个人临时配置（settings.json 入仓共享护栏，local 不入仓）
.claude/settings.local.json
