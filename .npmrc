# 供应链安全基线（harness 研究 #8：4 行挡 80% npm 攻击）
# 新发布的包静默等待 1 天，避开刚被投毒的版本窗口
minimum-release-age=1440
# 安装前对依赖做基本审计提示
audit=true
# 锁住 registry，防被改写
registry=https://registry.npmjs.org/
# pnpm：严格 peer，避免幽灵依赖
strict-peer-dependencies=false
